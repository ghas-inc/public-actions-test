on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello ${NAME}!!!" >> ${GITHUB_STEP_SUMMARY}
        env:
          NAME: ${{ secrets.MY_REPO_SECRET }}
